# Clustering Projects

Unsupervised learning projects. See [ROADMAP.md Phase 2.2](../../ROADMAP.md).

**Planned projects:**
- Customer / sensor segmentation (K-Means)
- Anomaly detection in time-series (DBSCAN)
- Dimensionality reduction on sensor data (PCA)
- Autoencoder-based compression of sensor signals
